# Story 0.1: 前端项目初始化

Status: ready-for-dev

## Story

As a 开发者,
I want 初始化 Vue 3 前端项目骨架,
So that 后续功能开发有统一的基础架构。

## Acceptance Criteria

1. **Given** 项目目录为空 **When** 执行初始化命令 **Then** 创建 Vue 3 + TypeScript + Vite 项目
2. **And** 安装 Element Plus、Pinia、Vue Router、Axios
3. **And** 配置路径别名 @/、Element Plus 自动导入
4. **And** 项目可正常启动 `npm run dev`

## Tasks / Subtasks

- [ ] Task 1: 使用 Vite 创建 Vue 3 + TypeScript 项目 (AC: #1)
  - [ ] 1.1 执行 `npm create vite@latest frontend -- --template vue-ts`
  - [ ] 1.2 进入 frontend 目录

- [ ] Task 2: 安装核心依赖 (AC: #2)
  - [ ] 2.1 安装 Element Plus: `npm install element-plus`
  - [ ] 2.2 安装 Pinia: `npm install pinia`
  - [ ] 2.3 安装 Vue Router: `npm install vue-router@4`
  - [ ] 2.4 安装 Axios: `npm install axios`
  - [ ] 2.5 安装自动导入插件: `npm install -D unplugin-vue-components unplugin-auto-import`

- [ ] Task 3: 配置 Vite (AC: #3)
  - [ ] 3.1 配置路径别名 @/ 指向 src 目录
  - [ ] 3.2 配置 Element Plus 自动导入
  - [ ] 3.3 配置 Vue Router 自动导入

- [ ] Task 4: 创建基础目录结构 (AC: #3)
  - [ ] 4.1 创建 src/api 目录
  - [ ] 4.2 创建 src/components 目录
  - [ ] 4.3 创建 src/composables 目录
  - [ ] 4.4 创建 src/router 目录
  - [ ] 4.5 创建 src/stores 目录
  - [ ] 4.6 创建 src/types 目录
  - [ ] 4.7 创建 src/utils 目录
  - [ ] 4.8 创建 src/views 目录

- [ ] Task 5: 配置入口文件 (AC: #3)
  - [ ] 5.1 在 main.ts 中引入 Element Plus 样式
  - [ ] 5.2 创建 Pinia 实例
  - [ ] 5.3 创建 Vue Router 实例（基础配置）
  - [ ] 5.4 创建 App.vue 基础结构

- [ ] Task 6: 验证项目启动 (AC: #4)
  - [ ] 6.1 执行 `npm run dev`
  - [ ] 6.2 确认项目在 http://localhost:5173 正常运行

## Dev Notes

### 技术栈要求（必须严格遵守）

| 技术 | 版本 | 用途 |
|-----|------|------|
| Vue | 3.x | 前端框架 |
| TypeScript | 5.x | 类型安全 |
| Vite | 5.x | 构建工具 |
| Element Plus | 最新 | UI 组件库 |
| Pinia | 最新 | 状态管理 |
| Vue Router | 4.x | 路由管理 |
| Axios | 最新 | HTTP 客户端 |

### 关键配置示例

**vite.config.ts 配置：**

```typescript
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'
import { resolve } from 'path'
import AutoImport from 'unplugin-auto-import/vite'
import Components from 'unplugin-vue-components/vite'
import { ElementPlusResolver } from 'unplugin-vue-components/resolvers'

export default defineConfig({
  plugins: [
    vue(),
    AutoImport({
      resolvers: [ElementPlusResolver()],
    }),
    Components({
      resolvers: [ElementPlusResolver()],
    }),
  ],
  resolve: {
    alias: {
      '@': resolve(__dirname, 'src'),
    },
  },
})
```

**main.ts 配置：**

```typescript
import { createApp } from 'vue'
import { createPinia } from 'pinia'
import router from './router'
import App from './App.vue'

import 'element-plus/dist/index.css'

const app = createApp(App)
app.use(createPinia())
app.use(router)
app.mount('#app')
```

**router/index.ts 基础配置：**

```typescript
import { createRouter, createWebHistory } from 'vue-router'
import type { RouteRecordRaw } from 'vue-router'

const routes: RouteRecordRaw[] = [
  {
    path: '/',
    component: () => import('@/views/Home.vue'),
  },
]

const router = createRouter({
  history: createWebHistory(),
  routes,
})

export default router
```

### 目录结构规范

```
frontend/
├── src/
│   ├── api/              # API 请求封装
│   ├── assets/           # 静态资源
│   ├── components/       # 公共组件
│   ├── composables/      # 组合式函数
│   ├── router/           # 路由配置
│   ├── stores/           # Pinia 状态管理
│   ├── types/            # TypeScript 类型定义
│   ├── utils/            # 工具函数
│   ├── views/            # 页面组件
│   ├── App.vue           # 根组件
│   └── main.ts           # 入口文件
├── index.html
├── package.json
├── tsconfig.json
├── tsconfig.node.json
└── vite.config.ts
```

### TypeScript 配置要求

```typescript
// tsconfig.json 关键配置
{
  "compilerOptions": {
    "target": "ES2020",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "strict": true,                    // 必须开启严格模式
    "skipLibCheck": true,
    "esModuleInterop": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "preserve",
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"]
    }
  },
  "include": ["src/**/*.ts", "src/**/*.d.ts", "src/**/*.tsx", "src/**/*.vue"],
  "exclude": ["node_modules"]
}
```

### Project Structure Notes

- 项目位于根目录的 `frontend/` 文件夹
- 后端项目将位于 `backend/` 文件夹（Story 0.2）
- 这是项目的第一个 Story，无前置依赖

### 反模式警告（必须避免）

```typescript
// ❌ 避免：使用默认导出（组件文件除外）
// export default ...

// ✅ 推荐：使用命名导出
export const useUserStore = defineStore('user', () => { ... })

// ❌ 避免：使用 var 声明
var x = 1

// ✅ 推荐：使用 const/let
const x = 1
```

### 测试框架说明

- 前端单元测试使用 Vitest（后续 Story 配置）
- 组件测试使用 @vue/test-utils
- E2E 测试使用 Playwright

### References

- [Source: _bmad-output/planning-artifacts/architecture.md#Selected Starters]
- [Source: _bmad-output/planning-artifacts/architecture.md#Initialization Command]
- [Source: _bmad-output/project-context.md#技术栈与版本]
- [Source: _bmad-output/project-context.md#目录结构规范]

## Dev Agent Record

### Agent Model Used

(待填写)

### Debug Log References

(待填写)

### Completion Notes List

(待填写)

### File List

(待填写)