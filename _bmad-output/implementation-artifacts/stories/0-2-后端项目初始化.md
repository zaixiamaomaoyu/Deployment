# Story 0.2: 后端项目初始化

Status: ready-for-dev

## Story

As a 开发者,
I want 初始化 Express 后端项目骨架,
So that 后续 API 开发有统一的基础架构。

## Acceptance Criteria

1. **Given** 项目目录为空 **When** 执行初始化命令 **Then** 创建 Express + TypeScript 项目
2. **And** 配置 tsconfig.json、nodemon、ESLint
3. **And** 创建 src 目录结构（controllers、routes、services、middlewares、models、types）
4. **And** 项目可正常启动 `npm run dev`

## Tasks / Subtasks

- [ ] Task 1: 初始化后端项目基础结构 (AC: #1)
  - [ ] 1.1 创建 backend 目录
  - [ ] 1.2 执行 `npm init -y` 初始化 package.json
  - [ ] 1.3 安装核心依赖：express、cors、dotenv、mysql2、@anthropic-ai/sdk、express-session
  - [ ] 1.4 安装开发依赖：typescript、@types/*、ts-node、nodemon

- [ ] Task 2: 配置 TypeScript 环境 (AC: #2)
  - [ ] 2.1 执行 `npx tsc --init` 生成 tsconfig.json
  - [ ] 2.2 配置 tsconfig.json 编译选项
  - [ ] 2.3 配置 nodemon.json 开发热重载
  - [ ] 2.4 配置 ESLint（基础规则）

- [ ] Task 3: 创建项目目录结构 (AC: #3)
  - [ ] 3.1 创建 src/config 目录（配置管理）
  - [ ] 3.2 创建 src/controllers 目录（请求处理）
  - [ ] 3.3 创建 src/routes 目录（路由定义）
  - [ ] 3.4 创建 src/services 目录（业务逻辑）
  - [ ] 3.5 创建 src/middlewares 目录（中间件）
  - [ ] 3.6 创建 src/models 目录（数据模型）
  - [ ] 3.7 创建 src/types 目录（TypeScript 类型）
  - [ ] 3.8 创建 src/utils 目录（工具函数）

- [ ] Task 4: 配置应用入口文件 (AC: #4)
  - [ ] 4.1 创建 src/app.ts 应用主入口
  - [ ] 4.2 配置基础 Express 应用设置
  - [ ] 4.3 配置 CORS 中间件
  - [ ] 4.4 配置 Session 中间件
  - [ ] 4.5 配置错误处理中间件

- [ ] Task 5: 验证项目启动 (AC: #4)
  - [ ] 5.1 配置 package.json scripts
  - [ ] 5.2 执行 `npm run dev`
  - [ ] 5.3 确认项目在 http://localhost:3000 正常运行

## Dev Notes

### 技术栈要求（必须严格遵守）

| 技术 | 版本 | 用途 |
|-----|------|------|
| Express | 最新 | Web 框架 |
| TypeScript | 5.x | 类型安全 |
| Node.js | 20.x | 运行环境 |
| MySQL2 | 最新 | 数据库驱动 |
| Anthropic SDK | 最新 | AI 服务集成 |

### 关键依赖清单

**核心依赖：**
```bash
npm install express cors dotenv mysql2 @anthropic-ai/sdk express-session
```

**开发依赖：**
```bash
npm install -D typescript @types/node @types/express @types/cors @types/express-session ts-node nodemon
```

### 目录结构规范

```
backend/
├── src/
│   ├── config/          # 配置管理（数据库、Session、AI）
│   ├── controllers/     # 控制器（请求处理）
│   ├── middlewares/     # 中间件（认证、错误处理、SSE）
│   ├── routes/          # 路由定义
│   ├── services/        # 业务逻辑（AI 服务、内容服务）
│   ├── models/          # 数据模型
│   ├── types/           # TypeScript 类型定义
│   ├── utils/           # 工具函数
│   ├── app.ts           # 应用入口
│   └── server.ts        # 服务器启动
├── .env.example         # 环境变量模板
├── package.json
├── tsconfig.json
├── nodemon.json
└── .eslintrc.json
```

### 核心配置文件

**package.json scripts 配置：**
```json
{
  "scripts": {
    "dev": "nodemon src/server.ts",
    "build": "tsc",
    "start": "node dist/server.js",
    "lint": "eslint src/**/*.ts"
  }
}
```

**tsconfig.json 关键配置：**
```json
{
  "compilerOptions": {
    "target": "ES2020",
    "module": "commonjs",
    "outDir": "./dist",
    "rootDir": "./src",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true,
    "declaration": true,
    "sourceMap": true
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist"]
}
```

**nodemon.json 配置：**
```json
{
  "watch": ["src"],
  "ext": ".ts,.js",
  "ignore": [],
  "exec": "npx ts-node ./src/server.ts"
}
```

### 应用入口文件示例

**src/app.ts 基础配置：**
```typescript
import express from 'express';
import cors from 'cors';
import session from 'express-session';
import dotenv from 'dotenv';

// 加载环境变量
 dotenv.config();

const app = express();

// 基础中间件
app.use(cors({
  origin: process.env.FRONTEND_URL || 'http://localhost:5173',
  credentials: true
}));
app.use(express.json());
app.use(express.urlencoded({ extended: true }));

// Session 配置
app.use(session({
  secret: process.env.SESSION_SECRET || 'your-secret-key',
  resave: false,
  saveUninitialized: false,
  cookie: {
    secure: process.env.NODE_ENV === 'production',
    httpOnly: true,
    maxAge: 24 * 60 * 60 * 1000 // 24小时
  }
}));

// 健康检查路由
app.get('/health', (req, res) => {
  res.json({ status: 'OK', timestamp: new Date().toISOString() });
});

export default app;
```

**src/server.ts 服务器启动：**
```typescript
import app from './app';

const PORT = process.env.PORT || 3000;

app.listen(PORT, () => {
  console.log(`🚀 后端服务器运行在端口 ${PORT}`);
  console.log(`🌐 健康检查: http://localhost:${PORT}/health`);
});

// 处理未捕获的异常
process.on('uncaughtException', (error) => {
  console.error('❌ 未捕获异常:', error);
  process.exit(1);
});

process.on('unhandledRejection', (error) => {
  console.error('❌ 未处理拒绝:', error);
  process.exit(1);
});
```

### 环境变量模板

**.env.example 文件：**
```
# 服务器配置
PORT=3000
NODE_ENV=development

# 前端地址（CORS）
FRONTEND_URL=http://localhost:5173

# 数据库配置
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=deployment_learning

# 微信 OAuth 配置
WECHAT_APP_ID=
WECHAT_APP_SECRET=

# Claude API 配置
ANTHROPIC_API_KEY=

# Session 配置
SESSION_SECRET=your-session-secret-here
```

### 项目结构说明

- 前端项目位于 `frontend/` 文件夹（Story 0.1 已完成）
- 后端项目位于 `backend/` 文件夹（当前 Story）
- 数据库初始化将在 Story 0.3 进行
- 环境变量配置将在 Story 0.4 完善

### 反模式警告（必须避免）

```typescript
// ❌ 避免：使用 require 语法
const express = require('express');

// ✅ 推荐：使用 ES6 import
import express from 'express';

// ❌ 避免：any 类型
function handleRequest(req: any, res: any) { ... }

// ✅ 推荐：明确的类型定义
import { Request, Response } from 'express';
function handleRequest(req: Request, res: Response) { ... }

// ❌ 避免：同步操作阻塞
app.get('/slow', (req, res) => {
  const data = fs.readFileSync('large-file.txt'); // 阻塞
});

// ✅ 推荐：异步操作
app.get('/fast', async (req, res) => {
  const data = await fs.promises.readFile('large-file.txt'); // 非阻塞
});
```

### 测试策略

- 单元测试：Jest + Supertest（后续 Story 配置）
- API 测试：Postman 集合
- 集成测试：数据库连接测试

### References

- [Source: _bmad-output/planning-artifacts/architecture.md#后端：Express + TypeScript（手动搭建）]
- [Source: _bmad-output/planning-artifacts/architecture.md#Project Structure]
- [Source: _bmad-output/planning-artifacts/architecture.md#Environment Configuration]
- [Source: _bmad-output/planning-artifacts/prd.md#API Specification]

## Dev Agent Record

### Agent Model Used

Claude Code CLI (LongCat-Flash-Chat)

### Debug Log References

### Completion Notes List

### File List

- backend/package.json (新建)
- backend/tsconfig.json (新建)
- backend/nodemon.json (新建)
- backend/.eslintrc.json (新建)
- backend/.env.example (新建)
- backend/src/app.ts (新建)
- backend/src/server.ts (新建)
- backend/src/config/ (新建目录)
- backend/src/controllers/ (新建目录)
- backend/src/routes/ (新建目录)
- backend/src/services/ (新建目录)
- backend/src/middlewares/ (新建目录)
- backend/src/models/ (新建目录)
- backend/src/types/ (新建目录)
- backend/src/utils/ (新建目录)